package routers

import (
	"{{.ModulePath}}/cmd/app/config"
	"{{.ModulePath}}/{{.TypesPath}}/shared/infrastructure/rest"
)

func GetWorkerRouter(config config.Config) rest.Router {
	subRouters := []rest.SharedRouter{}

	return rest.NewWorkerRouter(
		config.PathPrefix,
		subRouters...,
	)
}
