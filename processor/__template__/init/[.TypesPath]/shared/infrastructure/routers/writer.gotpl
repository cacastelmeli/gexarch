package routers

import (
	"{{.ModulePath}}/cmd/app/config"
	"{{.ModulePath}}/{{.TypesPath}}/shared/infrastructure/rest"
)

func GetWriterRouter(config config.Config) rest.Router {
	subRouters := []rest.SharedRouter{}

	return rest.NewWirterRouter(
		config.PathPrefix,
		subRouters...,
	)
}
